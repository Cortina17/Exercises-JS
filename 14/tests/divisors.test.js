const divisors = require("../scripts/divisors.js")

describe('test exercise fourteen', () => {
    test('Test if all the divisors of a given number are returned', () => {
        expect(divisors(6)).toBe[1,2,3,6];
    });
});
